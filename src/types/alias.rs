/// Alias for a smart contract [byte vector][Vec<u8>].
pub type ScBytes = Vec<u8>;

/// Alias for a smart contract [integer][i64].
pub type ScInt64 = i64;

/// Alias for a smart contract [string][String].
pub type ScString = String;
